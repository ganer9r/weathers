package com.ganer.weather2.weather;

/**
 * Created by shim on 15. 1. 15..
 */
public class WeatherData {
    /*
    protected RetroWeatherState state = null;
    protected RetroWeather weatherCoords = null;


    public RetroWeatherData gsetWeatherData(){
        return WeatherDataDao.get(app.getPref().weather(), app.getRest());
     }

    public RetroWeather getWeatherCoords(AppImpl_ app){
        Location l = app.getCurrentCoords();
        return KmaWeatherDao.getCoords(app.getPref().weatherCoords(), app.getRest(), l);
    }
    */

}
